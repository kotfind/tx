query = {
    SOI ~ column+ ~ EOI
}

column = {
    column_number
  | column_name
}

column_name = @{
    (LETTER | NUMBER | "_")+
}

column_number = @{
    natural_number
}

natural_number = @{
    ASCII_NONZERO_DIGIT ~ ASCII_DIGIT*
}

WHITESPACE = _{ " " | "\t" | "\r" | "\n" }
