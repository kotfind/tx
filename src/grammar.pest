query = {
    column+ ~ EOI
}

column = {
    column_number
  | column_name
}

column_name = @{
    (LETTER | NUMBER | "_")+
}

column_number = @{
    natural_number
}

// NOTE: including zero
natural_number = @{
    ASCII_DIGIT+
}

WHITESPACE = _{ " " | "\t" | "\r" | "\n" }
